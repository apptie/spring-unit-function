spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:13306/ddang_dev?serverTimezone=UTC&characterEncoding=UTF-8
    username: ddang_dev_user
    password: 1234
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    url: jdbc:mysql://localhost:13306/ddang_dev?serverTimezone=UTC&characterEncoding=UTF-8
    user: ddang_dev_user
    password: 1234

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
